[package]
name = "taper-validation-rust"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
taper-hashmap = { path = "vendor/taper-hashmap" }

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
xxhash-rust = { version = "0.8", features = ["xxh3"] }

[[bench]]
name = "taper_validation"
harness = false
